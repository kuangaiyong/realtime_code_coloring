target/
.run/
*.exec
*.log

.idea/
*.iml
.vscode/
.DS_Store

# 本机数据库凭据，绝不入库（见 CLAUDE.md 工具链依赖一节）
.env.local

# CodeGraph 的本地索引，随代码重建即可，不入库
.codegraph/

# E2E 脚本用 py_compile 做语法检查时留下的字节码
__pycache__/

# UI 参考截图：只作设计参考，几十 MB 的 JPEG 不进仓库
参考图片/

# 本机的 Claude Code 自定义命令与技能，属个人工具配置，与项目无关。
# 注意只忽略这两个子目录 —— .claude/launch.json 是项目的预览配置，要入库
.claude/commands/
# openspec 等插件生成的 skill 不入库；项目自己的 rtcc-* skill 要入库
.claude/skills/*
!.claude/skills/rtcc-*/

# OpenSpec：长期规格 specs/ 与项目口径 config.yaml 是资产，入库；
# changes/ 是过程草稿（做完即失效），不入库
openspec/changes/

# Codex 的仓库级 skills 是指向 .claude/skills/rtcc-* 的本机目录联接（单一事实源在 .claude 侧），
# 联接本身不入库；换机器用 New-Item -ItemType Junction 重建即可
.agents/