server:
  port: 18090

spring:
  application:
    name: coverage-platform

coverage:
  # 被测服务的 JaCoCo 探针地址（tcpserver 模式），可配多个实例。
  # 同一服务的多个实例各自持有计数器，聚合起来才是这个服务的真实覆盖
  instances:
    - localhost:6300
    - localhost:6301
  # 产物与源码目录：必须与探针所在进程的构建版本一致，否则 class id 对不上
  classes-dir: ../demo-service/target/classes
  source-dir: ../demo-service/src/main/java
  # 增量口径：去哪个仓库算 diff、默认与哪个基线比。
  # 默认与上一次提交比（本地开发场景）；接入 CI 时改为集成分支，如 main
  repo-dir: ..
  baseline: HEAD~1
  interval-ms: 3000
  timeout-ms: 3000

logging:
  level:
    com.coverage.platform: INFO
